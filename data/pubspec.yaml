name: data
description: "Data layer for clean architecture - contains data sources, repositories, models, and mappers."
version: 0.0.1
publish_to: 'none'

environment:
  sdk: ^3.1.0
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  
  # Local package dependency
  domain:
    path: ../domain
  
  # HTTP client
  http: ^1.2.2
  
  # Local storage
  shared_preferences: ^2.3.0
  sqflite: ^2.4.1
  sqflite_common_ffi: ^2.3.4+4
  path_provider: ^2.1.5
  path: ^1.9.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
