name: domain
description: "Domain layer for clean architecture - contains entities, repositories (abstractions), and use cases."
version: 0.0.1
publish_to: 'none'

environment:
  sdk: ^3.1.0
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
